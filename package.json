{
  "name": "administrador-tareas",
  "version": "1.0.0",
  "description": "Sistema de gestión de tareas con seguimiento de tiempo",
  "main": "index.js",
  "scripts": {
    "dev": "node get-ip.js && concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "dev:mobile": "node get-ip.js && concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd backend && npm run dev",
    "client:dev": "cd frontend && npm run dev",
    "build": "cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "ip": "node get-ip.js"
  },
  "keywords": [
    "tasks",
    "productivity",
    "time-tracking",
    "node.js",
    "react"
  ],
  "author": "Tu nombre",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "axios": "^1.12.2"
  }
}